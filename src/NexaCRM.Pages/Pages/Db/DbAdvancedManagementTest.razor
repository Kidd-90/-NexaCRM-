@page "/db/advanced"
<!--
  /db/advanced-test: onclick 이벤트 제거 버전(격리 테스트)
  목적: DOM 오류(InvalidCharacterError) 및 디자인 로딩 상태를 외부 요인 없이 확인
-->

<section class="space-y-6" aria-labelledby="page-title-advanced-test">
    <header class="space-y-4">
        <div class="flex flex-col gap-4 md:flex-row md:items-start md:justify-between">
            <div class="flex flex-col gap-3 md:gap-4">
                <div class="flex flex-wrap items-start gap-3 md:gap-4">
                    <div class="flex h-12 w-12 items-center justify-center rounded-xl themed-surface-card border border-[var(--border-color)] text-2xl" aria-hidden="true">
                        <span class="text-3xl">🧪</span>
                    </div>
                    <div class="space-y-2">
                        <h1 id="page-title-advanced-test" class="text-3xl font-bold themed-text-primary">고급 DB 관리 (테스트)</h1>
                        <div class="space-y-2 text-base themed-text-secondary">
                            <p>onclick 이벤트를 모두 제거한 테스트 전용 페이지입니다. 디자인/오류 상태 점검용.</p>
                            <div class="flex flex-wrap items-center gap-2 text-sm themed-text-secondary" role="status" aria-live="polite">
                                <span class="inline-flex h-2 w-2 rounded-full bg-emerald-500" aria-hidden="true"></span>
                                <span>테스트 모드: 모든 버튼의 onclick 제거</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="flex flex-wrap justify-end gap-2">
                <button type="button" class="db-action-button" disabled title="테스트: onclick 제거됨">
                    <span aria-hidden="true">🔖</span>
                    보기 저장
                </button>
                <button type="button" class="db-action-button" disabled>
                    <span aria-hidden="true">🧱</span>
                    컬럼
                </button>
                <button type="button" class="db-action-button" disabled>
                    <span aria-hidden="true">🧾</span>
                    일괄 작업
                </button>
                <button type="button" class="db-action-button" disabled>
                    <span aria-hidden="true">⬇️</span>
                    내보내기
                </button>
                <label class="db-action-button db-action-button--primary" for="importFileTest" title="테스트에서는 동작하지 않습니다; 클릭해도 아무 동작 없음.">
                    <span aria-hidden="true">➕</span>
                    고객 DB 추가하기
                </label>
                <input id="importFileTest" type="file" accept=".xlsx,.xls,.csv" class="hidden" />
            </div>
        </div>
    </header>

    <div class="themed-surface-card p-6 rounded-xl border border-[var(--border-color)] shadow-sm space-y-6">
        <div class="db-preview-banner" role="status" aria-live="polite">
            <span aria-hidden="true">ℹ️</span>
            <span>테스트 배너: 디자인 토큰과 RCL CSS 적용 상태를 확인하세요.</span>
        </div>

        <nav class="db-tabs" role="tablist">
            <span class="db-tab is-active" aria-current="page">목록</span>
            <span class="db-tab">세그먼트</span>
            <span class="db-tab">가져오기</span>
            <span class="db-tab">중복 관리</span>
            <span class="db-tab">등급 관리</span>
            <span class="db-tab">상담 구분값</span>
            <span class="db-tab">분배설정</span>
            <span class="db-tab">설정</span>
        </nav>

        <section class="explore">
            <aside class="filters">
                <div class="filter-group">
                    <label>상태</label>
                    <select class="form-select">
                        <option>전체</option>
                        <option>신규</option>
                        <option>진행</option>
                        <option>완료</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label>분배일(시작)</label>
                    <input class="form-control" type="date" />
                </div>
                <div class="filter-group">
                    <label>분배일(종료)</label>
                    <input class="form-control" type="date" />
                </div>
                <div class="filter-group">
                    <label>검색</label>
                    <input class="form-control" placeholder="이름/연락처" />
                </div>
            </aside>

            <div class="grid">
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>고객명</th>
                                <th>연락처</th>
                                <th>그룹</th>
                                <th>상태</th>
                                <th>액션</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>홍길동</td>
                                <td>010-1234-5678</td>
                                <td>A그룹</td>
                                <td><span class="badge bg-success">활성</span></td>
                                <td><span class="text-muted">(테스트) 삭제/수정 비활성화</span></td>
                            </tr>
                            <tr>
                                <td>김영희</td>
                                <td>010-9876-5432</td>
                                <td>B그룹</td>
                                <td><span class="badge bg-secondary">대기</span></td>
                                <td><span class="text-muted">(테스트) 삭제/수정 비활성화</span></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </section>
    </div>
</section>
